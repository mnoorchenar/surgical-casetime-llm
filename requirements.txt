# ============================================================
# Clinical Surgery Pipeline — Python dependencies
# ============================================================
# Install all:  pip install -r requirements.txt

# -- LangChain ecosystem (free API integrations) -------------
langchain-core>=0.3.0
langchain-google-genai>=2.0.0      # Gemini embeddings  (free tier)
langchain-huggingface>=0.1.0       # HuggingFace local embeddings
langchain-groq>=0.2.0              # Groq Llama LLM     (free tier)

# -- LangGraph (pipeline orchestration) ---------------------
langgraph>=0.2.0

# -- Embeddings (local, no API key) -------------------------
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.1.0

# -- ML models ----------------------------------------------
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
optuna>=3.6.0

# -- Deep learning (MLP) ------------------------------------
tensorflow>=2.15.0

# -- Data / utils -------------------------------------------
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.12.0
python-dotenv>=1.0.0

# -- Visualisation ------------------------------------------
matplotlib>=3.8.0
